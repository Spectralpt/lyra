stages:
  - build
  - test
  - deploy

default:
  image: alpine

build:
  stage: build
  script:
    - echo "Build!"

test:
  stage: test
  needs: [build]
  script:
    - echo "Test!"

deploy:
  stage: deploy
  needs: [test]
  script:
    - echo "Deploy!"
`
